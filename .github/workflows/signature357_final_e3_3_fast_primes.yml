name: Signature 357 final e3=3 primewise probes (manual fallback)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: signature357-final-e3-3-fast-manual
  cancel-in-progress: true

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: >-
          echo "Primewise public-Magma fan-out is disabled by default; use the
          serial low-memory probe workflow first and enable targeted primes only
          for a retained nonzero survivor space."
